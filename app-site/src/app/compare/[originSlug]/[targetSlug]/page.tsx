import Link from "next/link";
import { getRuntimeRepositories } from "@/server/database";
import { compareDensityEvidence } from "@/server/services";
export default async function ComparePage({ params }: { params: Promise<{ originSlug: string; targetSlug: string }> }) { const { originSlug, targetSlug } = await params; const { density } = await getRuntimeRepositories(); const result = await compareDensityEvidence(originSlug, targetSlug, density); if (result.status !== "ok") return <main><h1>Verified data unavailable</h1><p>Metroplist recognises this request but does not yet hold a verified result.</p><form action="/api/data-requests" method="post"><input type="hidden" name="origin" value={originSlug}/><input type="hidden" name="target" value={targetSlug}/><button type="submit">Request this comparison</button></form></main>; const { origin, target, directional } = result; return <main><p className="eyebrow">Density comparison · {origin.referenceYear}</p><h1>{origin.name} to {target.name}</h1><p className="lede">{origin.name} has {directional.ratioOriginToTarget.toFixed(2)} times the population density of {target.name}.</p><section className="metrics"><div><span>{origin.name}</span><strong>{origin.density.toFixed(1)}</strong></div><div><span>{target.name}</span><strong>{target.density.toFixed(1)}</strong></div><div><span>Absolute difference</span><strong>{directional.absoluteDifference.toFixed(1)} people/km²</strong></div></section><section className="details"><h2>Comparison frame</h2><dl><dt>Percentage relationship</dt><dd>{target.name} is {directional.targetAsPercentOfOrigin.toFixed(1)}% of {origin.name}</dd><dt>Mode</dt><dd>{result.comparisonMode.replaceAll("_", " ")}</dd><dt>Reference periods</dt><dd>21 March 2021 for both places</dd><dt>Boundary compatibility</dt><dd>Compatible: 2021 lower-tier local-authority geography</dd><dt>Evidence identity</dt><dd><code>{result.canonicalComparisonKey}</code></dd></dl></section><p>Each density is calculated by Metroplist from a Metroplist-derived population total, its official ONS TS001 component observations, and an official ONS 2021 land-area observation. It is not an ONS-published density.</p><nav><Link href={`/compare/${target.slug}/${origin.slug}`}>Reverse comparison</Link> · <Link href={`/density/${origin.slug}`}>View {origin.name}</Link></nav></main>; }
