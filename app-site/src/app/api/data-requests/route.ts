import { NextResponse } from "next/server";
import { getRuntimeRepositories } from "@/server/database";
export async function POST(request: Request) { const form = await request.formData(); const origin = String(form.get("origin") ?? ""); const target = String(form.get("target") ?? ""); const repositories = await getRuntimeRepositories(); const [originPlace, targetPlace] = await Promise.all([repositories.places.findPlaceBySlug(origin), repositories.places.findPlaceBySlug(target)]); const dataRequest = await repositories.dataRequests.createOrIncrement({ requestedPlaceAText: origin, requestedPlaceBText: target, resolvedPlaceAId: originPlace?.id ?? null, resolvedPlaceBId: targetPlace?.id ?? null, requestedIndicatorText: "POP_DENSITY_KM2", resolvedIndicatorId: "ind_population_density_km2" }); return NextResponse.json({ status: "requested", requestCount: dataRequest.requestCount, requestId: dataRequest.id }, { status: 202 }); }
