import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";
describe("embedded bootstrap", () => { it("matches the live Next and Vitest configuration", () => { const source = readFileSync("scripts/bootstrap-app-site.py", "utf8"); const packageJson = JSON.parse(readFileSync("package.json", "utf8")) as { dependencies: Record<string, string>; devDependencies: Record<string, string> }; const nextVersion = source.match(/NEXT_VERSION = "([^"]+)"/)?.[1]; expect(source).toContain("vitest.config.mts"); expect(source).not.toContain("vitest.config.ts"); expect(nextVersion).toBe(packageJson.dependencies.next); expect(nextVersion).toBe(packageJson.devDependencies["eslint-config-next"]); expect(nextVersion).toBe("16.2.12"); expect(source).toContain('"next": NEXT_VERSION'); expect(source).toContain('"eslint-config-next": NEXT_VERSION'); expect(source).not.toContain("16.2.10"); }); });
