# ADR 0004: No unreviewed source acquisition in public requests

## Status

Accepted.

## Decision

A public comparison request must not retrieve unknown external data and publish it immediately.

## Workflow

discover → retrieve → quarantine → validate → resolve geography → inspect methodology → approve → publish

Missing results become consolidated data requests and research demand.
